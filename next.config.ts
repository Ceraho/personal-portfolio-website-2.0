import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  /* config options here */
  images: {
    domains: ["images.unsplash.com", "github.com", "assets.aceternity.com"],

  }
};

export default nextConfig;
